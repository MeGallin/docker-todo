{
  "name": "docker-text-api",
  "version": "1.0.0",
  "private": true,
  "description": "Dockerised Express API for deployment on Render",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "auth:hash": "node scripts/generatePasswordHash.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "argon2": "^0.45.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14",
    "supertest": "^7.2.2"
  }
}
